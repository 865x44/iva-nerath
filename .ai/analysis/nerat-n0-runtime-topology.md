# Nerath N0 runtime topology — Scout A

## Evidence Metadata
- **evidence_base_sha:** `1ee221b39a8e7bf46e80305246293a7f627f78af`
- **reviewed_at:** `2026-07-24T13:38:40+05:00`
- **repo_path:** `/home/alx/.local/share/iva/worktrees/nerath-core-candidate-20260724T000000Z`
- **scope:** `agent/instructions`, `vault/PERSONA.md`, `agent/agent.ts`, `scripts/lib/menu/index.mjs`
- **invalidated_by:** Changes to base commit, Eve framework updates, or routing logic.

SCOUT_A_OK: Trace completed successfully.

- Composition: Static rules in `agent/instructions.md`; dynamic parts in `agent/instructions/05-language.ts`, `agent/instructions/10-map.md`, `agent/instructions/20-core.ts`, `agent/instructions/25-persona.ts`, and `agent/instructions/now.ts` evaluate on `turn.started`.
- Persona: Capped at 800 chars in `agent/instructions/25-persona.ts`, read dynamically from `vault/PERSONA.md`. Generated by a 10-question quiz in `scripts/lib/menu/character.mjs` writing the result.
- Construction: Provider-neutral design in `agent/agent.ts` using `defineAgent`. Models configured in `agent/provider.ts` for `codex`, `ollama`, `opencode`, and `openrouter`, wrapped in `withReasoningStripped`.
- Menu routes: `scripts/lib/menu/index.mjs` defines screens including root, character, and core. Pseudo-sids `mdl` and `thk` are intercepted for wizards.
- Config switches: `MODEL_PROVIDER`, `THINKING_EFFORT`, `AGENT_LANGUAGE`/`settings.json`, and `MEMORY_SEARCH_MODE`.
- Manifest: `.output/manifest.sha256` and `manifest.root.sha256` are compiled via `eve build` and stamped/verified by `scripts/build-info.mjs`.
- Proposed integration: add `agent/instructions/30-nerath.ts`, loaded through discovery, disabled by default through settings or an environment flag.
- Uncertainty: exact Eve discovery configuration is implicit; tests and scripts verify scans of `agent/instructions/` and `.output/`.

Primary must verify cited symbols and decide the integration point.
