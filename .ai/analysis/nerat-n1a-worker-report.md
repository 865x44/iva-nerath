# Nerat N1a Worker Report

## Mixed-Trajectory Correction
Addressed the delayed/mixed worker snapshot by keeping the required firewall rules and negative example directly in the prompt generation, and converting the test file to use multiple named top-level subtests to exhaustively verify the prompt constraints.

## Changed Paths
- `tests/memory-firewall.test.ts` (Modified)
- `.ai/analysis/nerat-n1a-worker-report.md` (Modified)
- `update_rollup.js` (Deleted from repository root)

## Synthetic-Data Boundary
Only synthetic data was used to execute tests. There were no side effects on the filesystem, `data/`, `vault/`, `.env`, real transcripts, Telegram content, or the network.

## Known Limitations
This test provides a prompt-only guarantee of the inclusion of the required firewall rules. It does not exert deterministic control over arbitrary model outputs generated by Eve/Claude at runtime.

## Test Results
**Exact Final Command:** `/usr/bin/node-24 --test tests/memory-firewall.test.ts`
**Fixture-Matrix Result:** Verified exactly one occurrence of each firewall rule, negative example, firewall marker, and forbidden fact in the daily prompt, and verified their absence in all other periodic prompts (weekly, monthly, yearly). Verified date and vault path remain correctly parameterized. Verified import has no side effects.
**Result/Count:** `pass 5`, `fail 0`, and `git diff --check` PASS

AGY_CODE_N1A_OK
